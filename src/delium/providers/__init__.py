"""External data provider adapters (Keepa, DataForSEO, review provider).

Not yet implemented — see docs/data-layer.md for the adapter contracts.
Nothing outside this package is permitted to call a provider API directly;
`ingestion/` is the only consumer.
"""
